<div class="thumbnail">
    <img src="{{user.avatar}}" class="img-circle"></img>
    <div class="caption">
        <h3>{{user.usercard.name}}</h3>
        <p>{{user.usercard.description}}</p>
    </div>
</div>
